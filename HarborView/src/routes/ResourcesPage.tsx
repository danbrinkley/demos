import { BookOpen } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function ResourcesPage() {
  return (
    <PlaceholderPage
      icon={<BookOpen />}
      title="Resources"
      description="The Resources catalog — organized by capability, with role-based journeys layered on top — is coming in a future Harbor View build."
    />
  );
}
